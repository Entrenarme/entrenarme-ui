// @flow
import React from 'react';
import styled from 'styled-components';

const MainContainer = styled.div``;

const Dot = styled.div``;

type Props = {};

const DotNavigation = (props: Props) => {
  return (
    <div>
      <p>hola</p>
    </div>
  );
};

export default DotNavigation;
